version: '3'

services:
  database:
    image: "postgres"
    # uncomment to get persistent data when starting up and shutting down the container
    #volumes:
    #  - database-data:/var/lib/postgresql/data/